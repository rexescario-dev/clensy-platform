// Not yet implemented. Domain types currently live inside each apps/api module
// (e.g. src/modules/bookings/domain/). Promote a type here only once a second
// app or service actually needs to share it — do not pre-emptively duplicate.
export {};
