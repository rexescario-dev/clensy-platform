# platform/graphql/directives

Not yet implemented. Reserved for cross-cutting GraphQL directives (e.g. `@auth`, `@requiresPermission`) once a module actually needs one — module resolvers would declare them via `@Directive(...)`, the transformer/interceptor logic lives here.
