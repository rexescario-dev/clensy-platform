# apps/web

Not yet implemented. Planned: the Clensy dashboard/site (React/Next.js + Tailwind), consuming `@clensy/ui` and `@clensy/client` against `apps/api`'s GraphQL endpoint. Out of scope for the current TypeORM-vs-Prisma comparison work happening in `apps/api`.
