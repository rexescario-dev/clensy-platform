# apps/worker

Not yet implemented. Planned: background jobs (booking reminders, notifications, payment reconciliation) — a natural first microservice extraction boundary once there's real async work to move out of `apps/api`.
